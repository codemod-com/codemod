import { describe, it } from 'vitest';

describe('Test Suite 1', () => {
    it('addition', () => {
        assert(1 + 1 == 2);
    });
});

describe('Test Suite 2', () => {
    it('subtraction', () => {
        assert(1 - 1 == 0);
    });
});